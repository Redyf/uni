#include <stdio.h>
#include<stdlib.h>

main() {
  int tamanho, i;
  float G1[tamanho], G2[tamanho], media[tamanho];
  printf("\nInforme o número de alunos (tamanho do vetor):");
  scanf("%d", &tamanho);
  for (i = 0; i < tamanho; i++) {
    printf("\nInforme a nota G1 do aluno %d:", i+1);
  }
}
